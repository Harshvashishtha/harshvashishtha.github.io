source "https://rubygems.org"

# Mirrors the gem set GitHub Pages builds with, so a local preview
# matches what gets published. Run: bundle exec jekyll serve
gem "github-pages", group: :jekyll_plugins

group :jekyll_plugins do
  gem "jekyll-sitemap"
end
